{
  "name": "opengym",
  "version": "1.2.4",
  "private": true,
  "packageManager": "pnpm@10.14.0",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "turbo run build",
    "test": "turbo run test",
    "test:watch": "turbo run test:watch",
    "build:mobile": "pnpm --filter opengym-mobile build:mobile"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "devDependencies": {
    "turbo": "^2.10.11"
  }
}
